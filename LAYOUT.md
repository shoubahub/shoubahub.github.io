# دستور تخطيط الشاشة — منصّة شعبة

الهدف: **هيكل موحّد يُولَد مع كل شاشة تلقائياً** — فلا تعود أخطاء الأبعاد/التمرير الأفقي.

## القاعدة الذهبية
لا تُعِد كتابة أي «هيكل» في ملف الشاشة (لا `body`، لا `.screen`، لا هوامش عامّة).
كلّها معرّفة **مرّة** في `public/shell.css`، وكل شاشة **ترثها**.

## ملفات الأساس (تُوصَل بهذا الترتيب في كل شاشة)
```
tokens.css   → المتغيّرات (ألوان/خطوط/أنصاف أقطار)
shell.css    → هيكل الشاشة (الأبعاد، منع التمرير الأفقي، مناطق الإجراء)
components.css → المكوّنات (حقول، منسدلة/لوحة سفلية، أزرار، تقدّم، بطاقات)
frame.css    → إطار الجوّال + سلوك الجوّال الحقيقي (يُوصَل أخيراً)
```

## إنشاء شاشة جديدة
انسخ `public/_template.html` واملأ المحتوى فقط. تولد صحيحة من أول مرّة.

## أنماط الشاشة الثلاثة (اختَر، لا تُنسّق)
| النمط | الأعلى | منطقة الإجراء | كيف |
|-------|--------|----------------|-----|
| ترحيب/دخول | شعار | متدفّقة تحت المحتوى | `<div class="actions">` |
| معالج/إدخال | تقدّم + رجوع | مثبّتة أسفل | `class="screen has-foot"` + `<div class="foot">` |
| تبويب رئيسي | عنوان | شريط تبويبات | `<div class="tabbar">` |

## حضور العلامة (قاعدة معتمدة 2026-08-14)
- **كل شاشة تحمل شعار المنصّة** بشكل مدمج احترافي، لا معلّقاً وسط فراغ:
  - **نمط المعالج/الإدخال:** الشعار داخل شريط التنقّل **بجانب نقاط المراحل** — `navrow > .brand` (شعار 50px + فاصل رأسي + شريط التقدّم). موجود في `_template.html` فيولد تلقائياً.
  - **شاشة الدخول:** الشعار الأفقي الكامل بالحجم المرجعي (mark 132 + word 150) — واجهة العلامة الأولى، لا يُصغَّر.
  - **نمط التبويبات:** يُحدَّد موضع العلامة عند بنائه (ترويسة القسم).

## منطقة واحدة = زخرفة واحدة (قاعدة ملزمة 2026-08-15)
**المبدأ:** لا تتقاطع زخرفتان/رمزان في مكان واحد. لا يُعالَج التداخل بالتلاشي، بل **بالمكان**: لكل زخرفة منطقتها الخاصة.
1. **الأسطح التفاعلية** (زرّ/حقل/منسدلة) تحمل **توقيعها وحدها** (صدى المجلّد الصغير) — لا زخرفة أخرى تدخل منطقتها.
2. **زخرفة الصفحة الكبيرة** (`.echo`) تسكن **منطقة محجوزة خالية** (شريط سفلي 26%) لا يدخلها أي عنصر تفاعلي؛ حافتها العليا تتلاشى برفق كي لا يظهر قطع حادّ.
3. **إن لم توجد منطقة خالية كافية** — كشاشات النماذج ذات الشريط المثبّت — **تُلغى زخرفة الصفحة نهائياً**: `.screen.has-foot .echo{display:none}`. توقيع الأسطح يكفي.
4. **الأسطح التفاعلية معتمة دائماً:** لا `opacity` على زرّ أو حقل (الشفافية تكشف ما خلفها فيبدو متّسخاً). **الحالات تُعبَّر باللون**: الزرّ المعطّل = رملي صلب + نص باهت.
5. **ترتيب الطبقات:** ‎-1 الزخرفة · 0 المحتوى · 1 الأسطح التفاعلية · 2 مناطق الإجراء (`.actions`/`.foot`) · 80+ اللوحة السفلية.
> عند إضافة أي زخرفة جديدة: **احجز لها منطقة خالية أولاً**. إن لم تتوفّر، فلا تُضَف. لا استثناء.

## التوقيع البصري (قاعدة نهائية 2026-08-15)
**المبدأ: التمييز بالتضاد لا بالتكرار.**
| العنصر | التوقيع |
|--------|---------|
| الزرّ العنبري `.cta` (الإجراء) | **نظيف بلا زخرفة** — قوّته في لونه وبروزه |
| الأسطح الورقية: `.paper` (حقول) · `.sdrop .trigger` (منسدلة) · `.btn-ghost` (ثانوي) | **صدى المجلّد** (el-echo.svg) همساً من الطرف — تلقائي عبر `::before` في components.css |
| الشاشة | **بلا زخرفة صفحة** — الشعار يحمل العلامة |
- أزرار مركّبة (كزرّ جوجل): ترث `.cta` وتضيف تخطيطها الداخلي فقط (`class="cta gbtn"`).
- لا يُضاف أي وسم زخرفة في ملفات الشاشات — كلّه من الأساس.

## قواعد ثابتة (مضمونة عبر shell.css/frame.css)
- العرض الأقصى `--screen-max: 430px` ويتوسّط.
- الهامش الجانبي `--gap-side: 26px` موحّد (استخدم `var(--gap-side)`).
- **لا تمرير أفقي** — `.screen{overflow:hidden}` + `html,body{overflow-x:hidden}`.
- منطقة الأمان العلوية (النوتش) محترمة؛ السفلية على الأشرطة المثبّتة.
- شريط الحالة الوهمي (`.statusbar`) = معاينة الكمبيوتر فقط، يُخفى على الجوّال.
- كل عنصر تفاعلي من `components.css` فقط.
- الأرقام: `<span class="num">` (خط IBM Plex معزول LTR).

## لغة اللون (قاعدة معتمدة 2026-08-16)
**ثلاثة ألوان بثلاث وظائف — لا لون بلا معنى، ولا نصّ أسود في هذه المواضع.**

| العنصر | اللون | الوظيفة |
|--------|-------|---------|
| عنوان الشاشة `.head .h` | `--shouba-navy` | لون العلامة — أعلى مرتبة |
| **قيمة اختارها المستخدم**: `.sdrop .val` · `.paper > input` · `.pick .tx b` · `.seg.on` | `--shouba-navy` | «ما أدخلتَه أنت» |
| تسمية الحقل `.field > label` | `--shouba-text-muted` | تدلّ ولا تنافس |
| الوصف `.head .s` · الإرشاد `.hint` · النائب `.ph` | `--shouba-label` / `--shouba-hint` | مرتبة أدنى |
| زرّ الإجراء `.cta` | العنبري | الفعل الواحد — لا ينازعه شيء |

- **الخلاصة:** الكحلي = العلامة وما اختاره المستخدم · الترابي = التسميات · العنبري = الفعل.
- **كلّها في `components.css`** — لا تُلوّن نصّاً في ملف شاشة.
- **التباين مقيس:** العنوان ٨٫٠ · التسمية ٥٫٨ · القيمة على الورق ٩٫١ (كلّها فوق AA).

## موضع العنوان ثابت (قاعدة ملزمة 2026-08-16)
**`.head` يبقى خارج `.content` دائماً** — أخاً لها مباشرةً تحت `.navrow`:
```html
<div class="navrow">…</div>
<div class="head">…</div>          <!-- ثابت في كل الشاشات -->
<div class="content center">…</div> <!-- هو وحده ما يبتلع الفراغ -->
```
- **السبب:** وضعه داخل `.content center` يجعله يتوسّط مع المحتوى، فيتغيّر ارتفاعه بحسب طول الشاشة ويقفز العنوان بين شاشة وأخرى (اكتشفه المستخدم على الجوّال).
- **المقياس:** `.head` يبدأ عند أسفل `.navrow` تماماً في كل الشاشات (٥٨px في مقاس الجوّال المرجعي).
- **حارس في `shell.css`:** لو وُضع بالخطأ داخل `.content`، يُعاد إلى الأعلى تلقائياً — القاعدة مفروضة بالكود لا بالتذكّر.

## توزيع المحتوى رأسياً (لا فراغ سفلي كبير، ولا التصاق بالأسفل)
المحتوى يملأ الارتفاع عبر flex، ويُوزَّع الفراغ حوله — **لا نضبط الفراغ يدوياً في كل شاشة**.
لفّ المحتوى بـ `<div class="content …">` واختر سلوكاً:
| الصنف | السلوك | متى |
|-------|--------|-----|
| `content center` | يتوسّط رأسياً — الفراغ يتوزّع أعلى وأسفل بالتساوي | محتوى قصير (دخول/ترحيب/رسائل) |
| `content between` | يوزّع المجموعات على كامل الارتفاع (أولى أعلى، أخيرة أسفل) | رأس + محتوى + تذييل |
| `content` (افتراضي) | من الأعلى | نماذج طويلة/معالج |
- فاصل مرن `<div class="spacer"></div>` = يبتلع الفراغ لدفع مجموعتين متباعدتين.
- القاعدة: إن رأيت فراغاً سفلياً كبيراً ⇒ لفّ بـ `content center`، لا تُضِف هوامش يدوية.

## مقياس المسافات
استخدم فقط: **8 · 12 · 16 · 22 · 30**. لا أرقام عشوائية.
